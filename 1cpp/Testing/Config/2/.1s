// ********************
//
Процедура ОбработкаПроведения(ПараметрСписок)
    Перем _Сам;
	
    _Сам = СоздатьОбъект("Документ_Новый2");

	фЗаменитьБазовыйОбъект = _Сам.ЗаменитьБазовыйОбъект("Документ.Новый2", Контекст);
                                            
	ПараметрСписок.Установить("фЗаменитьБазовыйОбъект", фЗаменитьБазовыйОбъект);
	ПараметрСписок.Установить("Объект", _Сам );
	
КонецПроцедуры
