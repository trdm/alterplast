Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции

Процедура ВыброситьИсключениеДляМопольногоРежима() Экспорт
	Если МонопольныйРежим() = 0 Тогда Возврат; КонецЕсли;
	Сам()._ВыброситьИскл("1C:Предприятие запущено в монопольном режиме. Тестирование прямого доступа к DBF невозможно");
КонецПроцедуры

Процедура ВыброситьИсключениеНаБазеSQL() Экспорт
	Если MetaInfoClasses.ЭтоSQL_Версия() = 0 Тогда Возврат; КонецЕсли;
	Сам()._ВыброситьИскл("Класс не предназначен для работы с базой SQL");
КонецПроцедуры

Процедура ВыброситьИсключениеНаБазеDBF() Экспорт
	Если MetaInfoClasses.ЭтоSQL_Версия() = 1 Тогда Возврат; КонецЕсли;
	Сам()._ВыброситьИскл("Класс не предназначен для работы с базой DBF");
КонецПроцедуры
