///////////////////////////////////////////////////////////////////////
//Предопределенная процедура - вызывается при первом открытии формы
Процедура ПриОткрытии()
	//глВосстановитьЗначенияФормы(Контекст);
	
	//Сообщить("РежимРаботы="+РежимРаботы())
	Если РежимРаботы()="Конфигуратор" Тогда
		Форма.ДобавитьСвойствоМикроформы("Вид","","ТипЗначения");
		//Число
		//Строка
		//Цвет
		//Событие
		//ТипЗначения
		//Переменная
	Иначе
		Вид=Сокрлп(Форма.ПолучитьСвойствоМикроформы("Вид"));
		Если ПустоеЗначение(Вид)=1 Тогда
			 ТЗаголовок.Заголовок("<Не задан вид документа!!!>");
			 //Сообщить("ТЗаголовок="+ТЗаголовок)
			 Форма.Обновить();
			 Возврат;
		КонецЕсли
		Попытка
			Док=СоздатьОбъект("Документ."+Вид);
		Исключение
			Форма.ТЗаголовок.Заголовок("<Не правильно задан вид документа: """+Вид+""" !!!>");
			 Возврат;
		КонецПопытки

		Параметр="";		
//		Параметр=Форма.Параметр;
//		Параметр["Конт"]=Контекст;
		Таблица.Инициализация("Документ."+Вид,Параметр);
	КонецЕсли
КонецПроцедуры

///////////////////////////////////////////////////////////////////////
//Предопределенная процедура - вызывается при закрытии формы
Процедура ПриЗакрытии()
//	Если СостояниеКлавиши(27) Тогда
//		СтатусВозврата(0);
//	КонецЕсли;
//	глЗапомнитьЗначенияФормы(Контекст);
КонецПроцедуры

///////////////////////////////////////////////////////////////////////
//Процедура обработки нажатия кнопки Закрыть - выполняет закрытие формы
Процедура ЗакрытьФорму()
	Форма.Закрыть();
КонецПроцедуры

///////////////////////////////////////////////////////////////////////
//Процедура обработки нажатия кнопки Выполнить
Процедура Выполнить()

КонецПроцедуры

