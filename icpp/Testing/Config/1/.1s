Процедура Обработать(ФлагПроведения, ПараметрЖурнал)

	КонтДокумента = глВзятьКонтекст(Контекст);
	ПредставлениеДокумента = КонтДокумента.Вид()+" № "+СокрЛП(КонтДокумента.НомерДок)+" от "+КонтДокумента.ДатаДок;

	Если ТипЗначенияСтр(ПараметрЖурнал) = "ЖурналВызоваМетодов" Тогда
		лТипПроведения = ?(ФлагПроведения = 1, "ОбработкаПроведения", "ОбработкаУдаленияПроведения");
		ПараметрЖурнал.ДобавитьЗапись("Модуль::"+лТипПроведения+"<"+ПредставлениеДокумента+">");
	КонецЕсли;
КонецПроцедуры

Процедура ОбработкаПроведения(ПараметрЖурнал)
	Обработать(1, ПараметрЖурнал);
КонецПроцедуры

Процедура ОбработкаУдаленияПроведения()
	//Сообщить("Модуль::ОбработкаУдаленияПроведения");
	Обработать(0, глЖурнал);
КонецПроцедуры