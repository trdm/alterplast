// Класс_ResultLoader - проверка универсальной загрузки данных
Перем _Вектор;

Перем Журнал // :ЖурналВызоваМетодов
			Экспорт;

//Число СобытиеЗагрузки_НачалоЗагрузки(КолвоКолонок, КолвоСтрок)
//Число СобытиеЗагрузки_ДобавитьКолонку(ИмяКолонки)
//Число СобытиеЗагрузки_ДобавитьДанные(Контейнер) - сюда передавать какой-нибудь контейнер, проще всего Вектор.
//Число СобытиеЗагрузки_ОкончаниеЗагрузки()

// -------------------------------- -------------------------------------
Процедура Конструктор()                                   
	_Вектор = СоздатьОбъект("Вектор");	
КонецПроцедуры // Конструктор

Функция Инит(НовыйЖурнал) Экспорт
	Журнал = НовыйЖурнал;
КонецФункции

Функция СобытиеЗагрузки_НачалоЗагрузки(КолвоКолонок, КолвоСтрок) Экспорт
	Журнал.ДобавитьЗапись("СобытиеЗагрузки_НачалоЗагрузки<"+КолвоКолонок+">,"+"<"+КолвоСтрок+">");	
	
	Возврат 0;
КонецФункции

Функция СобытиеЗагрузки_ДобавитьКолонку(ИмяКолонки, НомерТипа) Экспорт
	Журнал.ДобавитьЗапись("СобытиеЗагрузки_ДобавитьКолонку<"+ИмяКолонки+">,"+"<"+НомерТипа+">");	

	Возврат 0;
КонецФункции

Функция СобытиеЗагрузки_ДобавитьДанные(Контейнер) Экспорт
	Журнал.ДобавитьЗапись("СобытиеЗагрузки_ДобавитьДанные<"+Контейнер.Количество()+">");	
	
	Контейнер.Выгрузить(_Вектор);
	Возврат 0;
КонецФункции

Функция СобытиеЗагрузки_ОкончаниеЗагрузки() Экспорт
	Журнал.ДобавитьЗапись("СобытиеЗагрузки_ОкончаниеЗагрузки");	
	Возврат 0;
КонецФункции

Функция СобытиеЗагрузки_СообщениеОбОшибке() Экспорт
	Возврат "";
КонецФункции

// аналог методов Вектора
Функция Количество() Экспорт
	Возврат _Вектор.Количество();
КонецФункции

Функция Получить(Индекс) Экспорт
	Возврат _Вектор.Получить(Индекс);
КонецФункции