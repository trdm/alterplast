Перем 
		ОбъектТестирования,
		ИмяКласса,
		Журнал
		;
		
Функция Конт(Конт) Возврат Конт; КонецФункции // : Контекст
Функция Сам() Возврат Конт(Контекст); КонецФункции // : Контекст

Процедура Конструктор()
	//Сам().ПропуститьТест(, "Пока тестов нет");
	ИмяКласса = "РеестрWin";
	ОбъектТестирования = СоздатьОбъект(ИмяКласса);
КонецПроцедуры        

Процедура Тест_Создать2() Экспорт
	ОбъектТестированияВрем = СоздатьОбъект("РаботаСРегистромWin");
КонецПроцедуры

Процедура Тест_ОткрытьКлюч() Экспорт
	Сам = Сам();
	                                                    
	 //HKEY_CLASSES_ROOT\.exe
	Сам.ПроверитьРавенство(ОбъектТестирования.ОткрытьКлюч("HKEY_CLASSES_ROOT", ".exe", "KEY_READ"), 1);
КонецПроцедуры

Процедура Тест_ОткрытьКлюч_НеверныеПараметры() Экспорт
	Сам = Сам();
	                                                    
	Сам.ПроверитьРавенство(ОбъектТестирования.ОткрытьКлюч("1", "", "KEY_READ"), 0);
	Сам.ПроверитьРавенство(ОбъектТестирования.ОткрытьКлюч("HKEY_CLASSES_ROOT", "", "KEY_READ"), 1);
КонецПроцедуры